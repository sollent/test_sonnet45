# Frontend Environment Configuration - E2E Testing
# Used when running E2E tests with Playwright
# Test backend runs on port 8090 (see .env.docker.test)

VITE_API_BASE_URL=http://localhost:8090
VITE_APP_TITLE=Auth App (E2E Test)
